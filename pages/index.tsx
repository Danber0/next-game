import Layout from "components/Layout";
import Button from "components/Button";

const Home = () => {
  return (
    <Layout>
      <Button text="Игра на память" />
    </Layout>
  );
};

export default Home;
