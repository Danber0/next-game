export type CardType = {
  id: number;
  url: string;
};

export type filterType = {
  id: number;
  name: string;
};

export type GameType = {
  id: number;
  url: string;
  pathToImage: string;
  name: string;
  text: string;
  alt: string;
};
